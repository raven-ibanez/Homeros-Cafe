{
  "crons": [
    {
      "path": "/api/cron/db-health",
      "schedule": "0 8 * * *"
    }
  ],
  "cleanUrls": true,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}